import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.aplicaciondeprueba002',
  appName: 'aplicaciondeprueba002',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
